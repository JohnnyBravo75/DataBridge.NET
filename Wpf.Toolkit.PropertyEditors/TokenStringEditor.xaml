<UserControl x:Class="Wpf.Toolkit.PropertyEditors.TokenStringEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="26" d:DesignWidth="300"
             x:Name="editorRoot">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--
            Editable ComboBox: the user can type freely or pick a token from the dropdown.
            Items are the available tokens ({File}, {Data}, …) collected from previous steps.
        -->
        <ComboBox IsEditable="True"
                  Text="{Binding Value, ElementName=editorRoot, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  ItemsSource="{Binding AvailableTokens, ElementName=editorRoot}"
                  StaysOpenOnEdit="True"
                  IsTextSearchEnabled="False"
                  VerticalContentAlignment="Center">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" FontFamily="Consolas" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</UserControl>
